# Handle line endings automatically for files detected as text
# and leave all files detected as binary untouched.
# Line endings will be normalized to LF on check-in.
* text=auto

# Unix shell files will always have LF line endings on checkout.
*.sh eol=lf

# Special case
bin/spfile.js eol=lf
